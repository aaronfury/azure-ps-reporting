# azure-ps-reporting
A collection of PowerShell scripts that can be used to run reports in Azure.

## Azure Permissions Required
For the service principal, you must manually assign the following permissions manually
* Graph API
  - SecurityEvents.Read
  - SecurityEvents.ReadWrite.All